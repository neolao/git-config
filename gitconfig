[user]
    name = neolao
    email = contact@neolao.com

[color]
    diff = auto
    status = auto
    branch = auto
    ui = auto

[push]
    default = matching

[core]
    editor = vim
    pager = less -R

[alias]
    create-remote-branch = !sh -c 'git push origin origin:refs/heads/$1' -
    track-remote-branch = !sh -c 'git checkout --track -b $1 origin/$1' -
