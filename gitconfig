[user]
    name = neolao
    email = contact@neolao.com

[color]
    diff = auto
    status = auto
    branch = auto
    ui = auto

[push]
    default = matching

[core]
    editor = vim
    pager = less -R

[alias]
    create-remote-branch = !sh ~/git-commands/create-remote-branch.sh
    track-remote-branch = !git checkout --track -b $1 origin/$1
    create-tag = !sh ~/git-commands/create-tag.sh
    reset-file = !sh ~/git-commands/reset-file.sh
